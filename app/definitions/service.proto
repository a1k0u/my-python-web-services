syntax = "proto3";

message TaskRequest {
  int32 task_count = 1;
}

message TaskResponse {
  string task = 1;
  int32 error = 2;
}

service Task {
  rpc GetTask(TaskRequest) returns (TaskResponse);
}
